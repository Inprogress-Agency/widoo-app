# Copy to .env.local (never versioned). Builds read these variables from their EAS environment.

# Empty in development: the app calls the API on the machine that serves Metro, port 8080.
EXPO_PUBLIC_API_URL=
# Project API key of PostHog Cloud EU (phc_…): public, write-only. Empty: no analytics at all.
EXPO_PUBLIC_POSTHOG_KEY=
# DSN of the Sentry project (EU region). Empty: no crash report.
EXPO_PUBLIC_SENTRY_DSN=
# Environment of crash reports; builds get it from eas.json. Empty: development.
EXPO_PUBLIC_APP_ENV=
# Public Mapbox token (pk.…), public scopes only: it ships inside the app.
EXPO_PUBLIC_MAPBOX_TOKEN=
# Secret download token (sk.…, scope DOWNLOADS:READ). Optional since Mapbox opened its SDK
# downloads; the native build reads it from the environment only, never from the app config.
RNMAPBOX_MAPS_DOWNLOAD_TOKEN=
